# Google CloudEvents – PHP

This repository contains types for CloudEvents issued by Google.

## Prerequisites

- PHP 7.3 (or higher)

## Generate

Generate libraries:

```sh
./gen.sh
```

Observe the new files in `src/`.